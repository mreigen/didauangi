//= require controllers
//= require plugins/jquery.rating

angular.module("didauangi",[]).
config(["$routeProvider", function($routeProvider) {
  $routeProvider.when("/", {templateUrl: "<%= asset_path('selection-scroll.html') %>", controller: SelectionsCtrl}).
  otherwise({redirectTo: "/"});
}]);